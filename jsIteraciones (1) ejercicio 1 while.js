function mostrar()
{
	//while: evalua una condicion. La condicion tiene que ser verdadera.     
	var contador=0;	
	//contador=contador+1;//el contador enumera cosas. Al poner contador=0 estamos inicializando el contador desde 0.	
	while(contador<10)
	{
		contador ++;// suma de a 1 y es la que mas se utiliza
		//contador=contador+1;// poner esto arriba hace que primero cuente antes de mostrar(alert).
		alert(contador);
	}
}//FIN DE LA FUNCIÓN